"""Dependency-neutral content contracts.

Import the exact submodule that owns a contract.  This package root stays
deliberately empty so low-level engine modules do not pull registry, pack, or
tooling dependencies into the event kernel.
"""
