❯ sup boss read @claude_docs/VISION_HIDING_COVER_PLAN.md  and review the code for the layer 1 implementation - our task is in plan mode to develop a new markdown in the claude docs for the lighting system (layer 2). This is going to be a full implementantion with the shadowcast system and interactions with the hidin and spell system.

this are the lighting info from bg3 [Pasted text #1 +29 lines] this is the original bg3 implementation - [Pasted text #2 +22 lines] consider we made big changes because this engine DOES NOT have  
  directional line of sight - it is a sphere expanding with dijkstra from the entity  -- so I think we have to adapt a bit the system also for them this tag system is only for hiding while we want to resue it more generally . They way I would say it is that we might have a light_status enum going from      
  Magic Darkness\ Darkness \ Light Darkness \ bright \ very bright (or some better names?) - this should be a tile property and interact with the vision tags (we haveto add a vision tag for magic darkness because we currently do not have it) - darkvision shifts darkness to light darkness and light          
  darkness to bright. [remember that darkvision has a range so maybe we have to think this through better maybe more than enum we might need some models for the vision modes with their range in it??] right now we have a single range but that is the one for normal senses. Because we do not have directional  
  senses I think we need to move up the interactions with hiding. Very bright --> cannot hide at // all Bright (can not use the hide action if someone sees him) but can move with roll if already stealthing (or default would be impossible sneaking to a person) // mild darkness you can also hide with roll    
  if in los same behavior when not in los as now // darkenss you are out of lines of sight directly // magic darkness same as darkness but does not get downgraded by darkvision // truesight or seeing true magicc darkness (like the warlock feat) considers it as the real table. Also this makes me reason      
  that tiles should be able to store more than on light modifier and have a get method that solves them - we can start with a default to teh tile e.g. outdoor normal day could start with bright or sunny day all very bright while an indoor tile could be dark as default this should be a default flag , then   
  lights and spells add their own modifers which get resolved e.g. an area originally dark could have a light covering it and a clouyd fog spell resulting to darkness, when teh cloud spell expires it becomes bright when the light object is broken it becomes dark again. 
  To clarify my thoughts: tiles have a default lighting status and a list of light status applied to them and a method to resolve the current light status - maybe better than a list a dictionary so we can remove them? - similarly to blocking/vision there should be a method to get this status for a specific entity a subjective method because the vision types interact with this - the first component to interact with this new system are senses/gridmap however they are currently populated our objective is to models similary to what we currently do for stealth/invisibility at entity level but only at tile level for obscured tiles, Of course we have to ensure we do this cleanly with proper handling of dependencies. 
  The second system to interact is hiding with the rules above - we relax a bit bg3 rules because we do not have directional cones for los - very bright removes stealth - bright is current behavior - light darkness relaxes the actiosn prerequisite - darkens/magic darkness 
  Something we should conisder (happens in bg 3 as well) that we might want to let all entities always "see" the cells next to him at least for movement purposes so that a unit in magic darkenss is at least able to declare movement actions and try to get out of it otherwise when in darkenss senses would fully stop working and everything cascades down. also attacks etc so I think to be honest that for cells at 1 step of distance of the querying unit darkenss should always be reduced 1 step what you think? this could be an elegant way to solve it - the blinded condition (that could be applied by a default handler when entering a dark zone via e.g. a marker condition given by the light status of the area you enter in) would apply the other combat modifier effects without excplicit check of the tiles for downstream systems.
  Then tehre is the big question of how do we handle light and the event associated to light status change (and probably also to light senses change for an entity that then needs to recompute its senses after gtting truevision or something else with a spell). When where do we put lights? They probably need to interact at gridmap level? the good thing is that gridmap imports tiles and baseblock so both those that needs to receive the light enums and those that will have to receive conditions associated to them. Maybe we do not need to have a light loot and just use the event system to keep things in sync? Like when a light is created the first time we compute the lightened area and then any update is carried by the event system? We need of course to be able to turn on off a light and to move it. Then the other question are the handlers - do we want marker conditions for the entities entering an area based on their status? this is a bit difficult because of the subjective thingy so maybe for now we can avoid? hiding actions/handlers are enough for this? 