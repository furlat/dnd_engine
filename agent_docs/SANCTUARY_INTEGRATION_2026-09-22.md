# Sanctuary yin-yang presentation integration

The user requested production integration through the Godot task after noticing
Sanctuary missing from the latest batch. This supersedes its historical prototype
exclusion. Preserve the existing approved yin-yang artwork.

## Bounded plan and observable contract

Requested behavior: native Sanctuary ownership visibly applies, follows its
recipient through movement, persists until real removal, then clears. A failed
attacker Wisdom save blocks the actual attack without blood; a successful save
allows ordinary damage and blood while the ward remains. The warded creature's
hostile action removes Sanctuary through its existing handler.

Boundary/input: public cast, movement, attack and turn commands produce complete
subjective lineages; save them and replay through the existing Pygame condition
sampler. Expected output: correctly timed native ward membership, ordinary
resolution and blood where injured, rear/front yin-yang media following the body,
and clearance at actual removal, viewed by both participants in all four cameras.

1. Inspect the existing native handlers, condition recipe/schema, sparse media
   importer and source registration. Keep them as owners; no Sanctuary renderer.
2. Import source-owned lifecycle exports using the existing protection importer.
   The original six-second preview has a forced fade and explicitly lacks a
   sustainable hold. Godot has been asked only for that actual export gap,
   retaining palette, art, ground pivot and rear/front split.
3. Add explicit passive cast/condition authoring and a real review story using
   the existing clothed humanoid/blood fixture. Verify apply/hold/removal, movement,
   failed versus successful attack saves, and offensive-action break. Exercise
   expiry in tests without requiring a lengthy video of ten empty rounds.
4. Run focused saved-replay/presentation checks and inspect the resulting small
   paired four-camera gallery. Report native limitations separately from art.

No extra spell family, targeting framework, field renderer, runtime asset audit,
new class hierarchy, damage rule change or unrelated duration correction is planned.
The reported See Invisibility/True Seeing duration discrepancy is queued for its
own native study when that sensory-spell unit begins; this integration does not
claim those existing demonstrations validate their complete rules.

## Required independent reviews

Anti-slop reviewer: check the scope against real native behavior and source export
limits; reject fake gameplay, unsupported hold slicing and per-spell runtime code.
Anti-OOP reviewer: check that existing ECS condition/event owners, passive JSON,
shared importer/sampler and full lineage replay suffice; flag an actual ownership
violation or necessary missing shared capability.

## Review results

Both reviewers approve the bounded integration: shared condition UUID lifetimes,
sequence apply/hold and existing clear-mask support suffice. The recipe must select
real camera exports through `world_basis: E` and preserve declared pivots. Cast
release applies native membership; do not duplicate the same introduction as a
second cast effect.

The native review clarified the failed save is an expected canceled attack,
retaining its completed save child; no invented swing, hit or blood should follow.
The fixture now explicitly accepts this one expected cancellation. Movement and
successful injury retain the ward owner, and expiry uses ten recipient turn starts.

Existing native limits remain explicit: the ward listens to ATTACK declaration,
not every harmful SpellEvent; self-break requires an explicit enemy target at
ATTACK/CAST_SPELL effect and does not handle position-only AoEs. This visual unit
proves that current native surface, not complete Sanctuary rules coverage.

## Source imported and review correction

The authoring task delivered
`/home/tommaso/.codex/worktrees/1aac/dnd_engine/output/weapon-vfx/delivery-sanctuary-lifecycle-v1/manifest.json`.
The unchanged importer installed six declared phases. The original camera bases,
`[128,164.9504]` pivot, colors, ground motif, overhead symbol and matching hands
remain. Apply is six seconds of formation/settling; it does not lock casting.
The 5.25-second source hold repeats without reintroducing the reveal. Removal
multiplies the current phase by the delivered 0.8-second mask. Both clocks already
exist in shared playback. The new color media has 101 packed PNG pages / 3,908,284
bytes; its clear mask reuses the existing Mage Armor source envelope.

Only passive authored spell/condition data and review fixtures were added. Native
rules, importer, condition sampler and Pygame renderer are unchanged. Rear media
uses the existing ground attachment and front media uses body attachment; real
camera rows use the existing world basis mapping.

The first four clips correctly covered hostile break and native expiry, but both
removed the ward before its six-second introduction finished. The final small
review instead pairs hostile break with a maintained-ward story: it ends after
real damage with the ward still active, then the ordinary 14-second review tail
shows the full maintained cycle. It adds no synthetic event or simulated turn.
Ten-turn expiry remains covered by native/replay tests and its earlier capture.

## Delivered result

[Open the four-clip Sanctuary review](http://127.0.0.1:8767/runs/20260922T123011Z-44b2f6/index.html): hostile-action break and maintained
ward, each from both actual subjective participants with four camera corners.
The existing fourteen-spell/barrel gallery remains separate and unchanged.

Verification: the broader selected gameplay/import/lifecycle run passed 60 tests.
After adding sustained-review coverage, all 33 selected Sanctuary/persistent-gameplay
tests pass; changed-module typing is clean. All four final saved-input captures
pass with zero presentation gaps. The browser loads all four and exports the
maintained clip with its exact saved input/trace, without JavaScript errors.

Evidence lives in `.runtime/sanctuary-20260922/`: `verified.log`, `final-tests.log`,
`typing.log`, `final-review.log`, `browser.log` and `visual-review/`.
Native expiry was also captured in `20260922T122558Z-6d8e30`; the final gallery
uses `20260922T123011Z-44b2f6`. Automated validation and agent visual inspection
are not human visual approval. Existing native hostile-spell/AoE limits described
above remain outstanding; no general rules-completeness claim is made.
