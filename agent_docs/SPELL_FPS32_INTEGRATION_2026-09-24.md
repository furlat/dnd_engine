# Spell media at 32 FPS — production integration

## Scope and observable contract

Integrate the user-authorized Godot delivery at
`/home/tommaso/.codex/worktrees/1aac/dnd_engine/output/weapon-vfx/spell-fps32-delivery/HANDOFF.md`.
The boundary is the same saved subjective event sequence rendered through the
existing presentation system. Reduce selected spell media above 32 FPS; use
the dense original Fireball export to increase its explosion from 24 to 32 FPS.
Actor animations, release/contact timing, gameplay, visibility, spatial ownership,
target anchors, camera registration, color and geometry retain their meaning.
The five newly integrated two-second clouds and their corrected renderer stay intact.

## Implementation sequence

1. Preserve the current 34 affected data files and the prior private art release.
   Preserve the complete handoff privately. Record focused baseline test results
   and existing saved-input review cases; do not generate different gameplay to
   disguise a rendering regression.
2. Obtain independent anti-slop and ECS/anti-OOP reviews of this plan and the
   supplied metadata/clock edits. Review the effective consumers, including
   application-to-sustain transitions, explicit FPS, time maps, held windows,
   directional selection and below-32 phases sharing a converted asset.
3. Run the supplied installer in dry-run mode, compare its planned records with
   current records, then apply the guarded metadata merge and separate payload.
   No new rendering executor, codec, startup scan or runtime hash audit.
   Source selection may round a finite media duration by less than one 32 FPS
   tick; elapsed-time anchors must not move. Fireball uses genuine dense samples.
4. Run affected presentation/media/condition/movement/spatial tests. Inspect
   failures before editing expectations: preserve actual behavioral assertions;
   derive source-frame positions from authored clocks where an old literal
   assumed 144 FPS. Add only missing meaningful clock-boundary coverage.
5. Replay saved cases from all four cameras, retaining paired observers where
   recorded: Fireball walls/Globe, Gust formation/hold, Sleep, Sacred Flame,
   Shield, Chill Touch, Aid, Bless/Bane/False Life transitions, Grease and a
   cloud ownership control. Inspect actual frames and lifecycle boundaries.
   Keep the separately documented cloud propagation limitation separate.
6. Remove only superseded media addresses that no current data reference uses.
   Existing atlas sharing and paired footpoint/XYZ references count. Retain
   originals in the archive and prior tagged release; do not prune that archive.
   Use the existing offline receipt and supplied file sizes for release accounting.
7. Request final reviews. Create the local private production release and
   matching install receipt after validation; update ASSETS.md and the recovery
   status with actual installed bytes, test evidence and the review gallery.

## Non-goals

No additional frame-rate campaign, cloud loop authoring, speculative packing
format, gameplay rule adjustment, new visual test framework, remote publication
or public Git history rewrite. Static condition layers remain static. Missing
unused catalog directions/tails and orphan thorn2 are not restored as content.

## Status

The corrected delivery is installed. Independent anti-slop and ECS reviews approve.
They caught and verified three data corrections before installation: fractional
jump contact frames preserve exact takeoff time; converted travel updates its
effective enclosing projectile FPS; Poison Spray inherits its phase asset ID
and needs all three clock fields updated. No runtime code or schema changed.

The baseline passed 577 tests. The full presentation run completed with 2,620
passed and 12 failures; all twelve were investigated and corrected. Thirteen
test files now address the selected clocks and packing without changing the
observable elapsed-time contracts. Four compile/sample checks protect effective
projectile clocks. The floor-clipping tests retain their original elapsed moments
and nonempty below-floor pixel probes; preparation/volley timing remains fixed.
Aid retains its explicit four-second period and unchanged crossfade interval.

The remaining failures identified obsolete camera-number filenames, Silence's
old 144 FPS frame indices and center-floor visibility requirement, and an importer
test that assumed the legacy source still owned Ice Knife travel. Real four-view
decoding replaces the filename check; the distinct-color importer fixture still
checks camera identity. An observed Silence sphere remains visible without its
center floor, but disappears with no current observation. Import tests now cover
both owned-source replacement and preservation of the selected packed asset.
All four final test-file corrections passed independent anti-slop review; their
21 contract tests pass. The final packed-only run passes **498 tests**, including
all previously failing cases, after removing superseded payloads. The broad
suite was not repeated: its 2,620 passes plus the corrected, rerun cases are the
recorded evidence. No production behavior was changed to satisfy a test.

All 28 saved-event replays pass with zero gaps and exactly equal input sequences.
The [review](http://127.0.0.1:8767/runs/20260924T131954Z-spell-fps32/index.html)
includes both observers and all four cameras. Fourteen compiled timelines retain
release, damage/contact and arrival dates. The delivery's 4,248 decoder samples
and an independent 90 installed/source samples retain pixels, offsets, blend,
XYZ/ownership and paired Grease footpoints. Representative final frames were
inspected for Fireball walls/Globe, maintained Gust, Sleep, Aid, Shield, Chill
Touch, Grease and the unchanged cloud edge/Globe controls.

The reviewed release removes 5,157 superseded files and retains 12 shared
blood footpoint pages. It adds 1,149 files, resulting in 4,430 files / 2,669,207,235
bytes instead of 8,438 / 4,285,436,828. This includes Fireball growing from
998,477,981 to 1,267,137,373 bytes for genuine 32 FPS source samples. Both installed
copies have exactly the manifest's file set and sizes. The local private release
is committed and tagged `spell-fps32-20260924`; no remote publication occurred.
Main-branch code/data changes remain in the working tree and must stay paired
with this asset release. The complete
handoff is preserved at `/home/tommaso/Dev/neurodragon_art/sources/spell-fps32-20260924/`.
Evidence and before-data copies are in `.runtime/spell-fps32-integration/`.
