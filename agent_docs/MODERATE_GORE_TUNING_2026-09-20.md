# Moderate gore increase — September 20

**Superseded after user visual rejection.** This tuned the wrong compact
reference. See [the correction](BLOOD_REFERENCE_CORRECTION_2026-09-20.md).

The user wants fuller gore than the compact gallery, without the exaggerated
wide skill example. The blood-art task confirmed the current templates are up
to date. The small result comes mainly from the authored receiving geometry.

## Bounded implementation

Tune the existing three native release patterns: ellipse radii approximately ×1.35 and
forward/lateral center offsets approximately ×1.15 (rounded authored values), preserving the two/three/one-region shapes.
This increases full-patch area about 82%, without increasing particle count,
adding a layout system, or changing shaders. First hit remains a smaller stain;
ordinary blood still reaches its existing cap after five qualifying injuries.
The current 28-contribution templates, dark palette and critical transient detail
remain the presentation data.

A read-only probe found identical receiving tile membership before/after for
all three patterns across the eight canonical directions (24 comparisons).
Existing native propagation, support-height admission, recorded receiving tiles
and subjective clipping continue to determine actual coverage, including other
angles. This is an authored footprint change, never a renderer-only stretch.
Check native corrosive/dread entry behavior with the wider recorded geometry.

Keep existing non-blood quantity caps and poison's artwork-only status. Do not
silently introduce poison mechanics or lower the first-hit bone/demonic amount
by changing the normalization cap. Add explicit repeated bone/demonic captures
so their current saturation is visible; these are not claimed as growing piles.
Include blood single hits by family/critical, seven-hit piercing and slashing,
and native repeated counterpart hits. Both observer views and four cameras.

## Validation

Use real commands and new captures because native geometry changes. Retain the
previous saved gallery for comparison. Run the existing native body-release,
corrosive/dread, retained replay and floor/particle geometry tests. Inspect
composed first-hit and saturated frames. No new tests merely restate constants.
Ask anti-slop and anti-OOP reviewers to review this bounded data change; no new
handlers, ownership, registries, events, or generic framework are proposed.

## Review and results

Independent anti-slop and anti-OOP reviewers approved the bounded change. Both
required honest saturation labels for non-blood materials and warned against
claiming unchanged membership at every possible angle. The final rounded
values preserve membership in all 24 canonical pattern/direction comparisons.
A 360-angle geometry probe remains local: piercing reaches 2–4 candidate tiles,
slashing 3–6 and blunt one, before native wall/height admission.

The blood-art author independently agreed that approximately 35% larger radii
and 15% larger offsets are a reasonable moderate increase. The optional suggestion
of 10% thicker airborne droplets was deferred after inspecting the current
rendered candidate; no particle/template/palette edits were necessary.

Final full-patch areas are about 1.81× the previous piercing/slashing shapes and
1.85× blunt. This is geometric area, not a claim that pixel coverage or every
frame's visible gore changes by that exact ratio. First-hit and seven-hit
four-camera footage were inspected, including a direct old/new crop comparison.
The saturated blood pool is visibly fuller while remaining close to the victim.

**101 relevant tests passed** in 22.55 seconds: native body residues, public
body/dread replay, geometric floor playback, body release media and legacy
particle compatibility. Existing door/height/hidden-view, corrosion and retreat
assertions remain green. The implementation changes only the six ellipse records
and adds/selects review data; no renderer or gameplay-execution code was added.


[Final gallery](http://127.0.0.1:8767/runs/20260919T235102Z-e50471/index.html): **28/28 clips passed**, 5,024 four-camera frames, both native observer views, no reported media gaps. All four new repeated counterpart/slashing cases contain seven actual attack nodes. Old comparison gallery `20260919T233559Z-0ebdbd` remains available.
