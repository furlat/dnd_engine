# Two-second cloud delivery integration

## Requested outcome and boundary

Integrate the user's approved September 24 cloud handoff to reduce production
storage and frame working sets without regressing the corrected rendering.
The observable boundary is saved subjective events replayed into four-camera
Pygame output. Inputs, gameplay, perceived cells, geometry and timeline ownership
stay unchanged. The new approved artwork and its formation/loop duration change.

Source: `/home/tommaso/.codex/worktrees/1aac/dnd_engine/output/weapon-vfx/cloud-two-second-review/production-handoff/HANDOFF.md`.
Five spells: Fog Cloud, Stinking Cloud, Cloudkill, Darkness, Incendiary Cloud.
Insect Plague is excluded by the author; retain its installed banks and timing.

## Baseline and invariants

- Retain the private `cloud-xyz-32fps-20260924` release, saved input sequences and
  the approved [ownership correction review](http://127.0.0.1:8767/runs/20260924T112844Z-cloud-ownership-correction/index.html).
- Preserve `sphere_field_owners`: full declared geometry assigns decorative
  fringe owners before subjective admission and before presentation translation.
  Never choose nearest visible owners or use the native circle as a pixel stencil.
- Preserve genuine XYZ, numerical camera registration, cropped pivots, normal
  blending, actual wall depth, observed Globe surfaces and spherical exclusions.
- No backend/sensory rule changes. The separately documented line-of-effect vs
  connected-propagation wall question remains pending, not part of this import.
- Maintain native movement speed and lifetime, the independent presentation
  clock, and the current-phase 630 ms smoothstep clear.

## Work sequence

1. Preserve current bindings and runtime helpers in the local review directory.
   Keep previous assets in their private release. Do not rewrite/delete originals.
2. Ask the original Godot task for fresh paired XYZ/ownership packets for all 28
   approved camera/motion banks. Colors alone are insufficient. Validate source
   Y, color/position correspondence, all camera bases, coverage, crop offsets,
   frame order and the diagnostic endpoint. Old XYZ cannot accompany new color.
3. Independently review this plan for anti-slop and ECS/data ownership. Reviewers
   must assess the requested visible behavior, not assume checks prove appearance.
4. Stage the complete delivery privately. Reuse `import_volume` and existing
   `PackedSurfaceFrames`. Each installed stationary bank contains 128 frames:
   apply [0,64), hold [64,128), 32 FPS. The importer splits these into phase-local
   assets: runtime holdStartFrame remains 0, holdFrames becomes 64. Exclude
   duplicate diagnostic frame 128.
   Update media references and the five authored hold ranges together. Keep
   recipes, scale, radius, clear envelope and movement speed intact.
5. Movement variants require evidence before runtime use. Cloudkill rolling has
   a source +X directional bias, not eight travel directions. The safe existing
   behavior translates the stationary surface in arbitrary directions. Preserve
   all delivered rolling banks privately; do not add heading machinery, mismap
   cameras, rotate only color or claim unproven switches are smooth. This unit
   installs the 20 stationary camera banks and retains translated stationary
   playback. Rolling selection is a separate evidence-gated change, not a
   prerequisite for the optimization. Do not import rolling manifests under
   stationary spell identities: the existing importer would overwrite them.
6. Validate the production import through registered media, including RGBA/XYZ
   correspondence and pivots. Measure total archive bytes, decoded sample size
   and representative cold/warm decode cost, separately from whole-game startup.
   No runtime source audits or hashes. Final savings include XYZ/ownership;
   color-only estimates are not production savings.
7. Run existing ownership/movement, physical occlusion, spatial suppression and
   lifecycle tests. Remove obsolete fixed frame assumptions only by expressing
   the same observable requirement with the authored duration. Preserve whole
   image equality against the selected source frames and hidden-owner probes.
8. Replay the same saved open-edge, movement, real-wall and Globe scenarios with
   both observers and all four cameras. Include all five new clouds, raised
   support, formation, repeated loop wraps and clear. Inspect actual frames and
   movement/loop seams; passing event checks alone do not approve art. Keep the
   prior clips available for comparison. Label the known propagation limitation.
9. Have both reviewers examine the final delta and evidence. Publish the local
   private production release only after the integration checks pass; preserve
   the full new source/export delivery and previous release. Update ASSETS.md
   and the recovery status with measured results and the review link.

## Completion evidence

Anti-slop and ECS reviewers independently approve the stationary data-only
integration. Both reject new rolling selection machinery for this unit and
confirm that existing clocks/importer already support the new windows. Replace
the old removal test's fixed 6000 ms formation assumption with the selected
application duration. Final review still requires matching packets and pixels.

Exports and installation are complete. Both independent implementation reviews
approve the bounded data-only change. All 24 saved-event replays are complete;
the unchanged renderer is installed with the validated new packets.


## Installed result and verification

The five stationary banks are installed through the unchanged importer. No
runtime Python, native events, schema, camera logic or compositor changed in
this unit. Only their ten media phases and five hold lengths changed; Insect
Plague and every other record compare equal to the retained pre-import metadata.

- Imported apply indices 0–63 and hold indices 64–127; runtime hold starts at 0.
- All 2,560 frames were independently checked through `registered_media_samples`
  against approved color crops: exact RGBA and pivots, aligned ownership, finite
  genuine Y and no saturated covered coordinates.
- 276 focused tests pass. An isolated run restoring the previous broken stencil
  makes all 12 whole-cloud ownership/movement regressions fail with the NEW
  assets too. The actual renderer stays unchanged and passes those same tests.
- New production surface packets total **635,672,033 bytes**, previously
  **1,962,894,890 bytes**: **1,327,222,857 bytes / 67.6% saved** for these five.
- Eight optional rolling banks total 288,486,822 bytes and remain privately
  archived, unbound. The public runtime does not claim directional support.

| Spell | Old archives MB | New archives MB | Old hold decoded MB/view | New hold decoded MB/view |
|---|---:|---:|---:|---:|
| fog_cloud | 464.0 | 145.0 | 672.6 | 241.4 |
| stinking_cloud | 421.0 | 130.2 | 580.9 | 208.3 |
| cloudkill | 467.4 | 146.1 | 672.6 | 241.4 |
| darkness | 263.4 | 81.0 | 374.8 | 130.3 |
| incendiary_cloud | 347.1 | 133.2 | 393.7 | 238.6 |
| insect_plague | 41.0 | 41.0 | 691.9 | 691.9 |


Measurements are decimal MB. Decoded hold size is the sum of all source frames
for one camera, not whole-game RAM. The per-frame source remains about 2–4 MB;
shorter loops reduce the working set rather than silently reducing resolution.
Measured Fog/Cloudkill hold loops now fit within the existing 640 MiB source
cache (241 MB instead of 673 MB per view). Their warm second decode lap is
roughly 0.7–1.0 ms TOTAL versus 1.4–1.6 seconds of repeated decoding previously.
This is source decode only, not game turns or complete rendering. Simultaneous
views/effects compete for the same bounded cache; no universal FPS claim follows.
No cache limit, startup audit or asset hash scan was added.

The full approved review and fresh XYZ delivery are preserved under
`/home/tommaso/Dev/neurodragon_art/sources/cloud-two-second-20260924/`, including
native captures, source recipes and unbound rolling variants. The preceding
private release and review clips remain available.

The private release is `cloud-two-second-20260924`, commit `488973a8`, with
8,438 installed files / 4,285,436,828 bytes. All art stays ignored in the public
code checkout; the private release and installation receipt agree. Previous
release tags are retained. Evidence and ad-hoc measurement scripts live in
`.runtime/cloud-two-second-integration/`. The final gallery is linked below.

Both independent implementation reviewers approve. Their additional checks
confirmed all 20 installed archive member lists and 80 complete installed
RGBA/XYZ/ownership packets against the private delivery. The clock/pixel tests
retain their observable requirements. Measurements describe source-cache cold
decode, not a flushed OS disk cache or a whole-scene FPS guarantee.


## Completed review

[24-clip production review](http://127.0.0.1:8767/runs/20260924T123138Z-two-second-clouds/index.html): both subjective observers and all
four cameras. All replay checks pass with zero presentation gaps. The saved
sequence values compare equal before/after for every clip; no native scenario
was rerun to manufacture different results. The page links the preceding
approved review for comparison. The review server was restarted on port 8767.

Inspected moving open edge over six timestamps, Fog/Globe from both observers,
Darkness/Globe, the level-8 Incendiary/ordinary-Globe control, actual Cloudkill
wall occlusion, Fog raised formation/removal, repeated Stinking Cloud phases,
and all four wall variants. New/old Fog and Incendiary wall frames were compared:
the already documented native propagation artifacts remain, with no new rule or
render workaround. These cases are labeled accordingly. Human visual approval
is still distinct from passing tests and this inspection.

Both independent implementation reviews approve. The public checkout has no
tracked runtime art. The local private art repository is clean at `488973a8`,
tag `cloud-two-second-20260924`; its manifest equals the installation receipt.
Source archive, prior production tags and optional rolling variants are intact.
No code-history changes or remote publication were part of this unit.
